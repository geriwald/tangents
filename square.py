import matplotlib.pyplot as plt
import numpy as np

l = 10
X = np.linspace(-l, l, 1000)


fig = plt.figure(figsize=(15, 15))

y = zip(x, 0)

for x in X:
    plt.plot()

plt.plot(x, 0, color='b', linewidth=0.05)

plt.show()
